# Dependencies
node_modules/
.pnp
.pnp.js

# Build
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
!.env.example

# Deployment config (tenant-specific)
# Not: tek-tenant Parla deploy için repoya commit ediliyor (secret içermez, sadece env var ref'leri + feature flags)
# config/.saha-config.json

# Testing
coverage/
playwright-report/
test-results/

# Supabase
.supabase/
supabase/.branches
supabase/.temp

# Lokal-only Parla baseline (kişisel makinede regen edilir, prod'a push edilmez)
supabase/migrations/00000000000000_parla_baseline_local.sql
supabase/local/parla-schema-baseline.sql

# Logs
logs/
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Temporary
tmp/
temp/
*.tmp

# Lokal-only / dev artifacts
.claude/settings.local.json
saha-app.zip
ekran görüntüsü navigasyon/

# Android signing — ASLA commit etme
android/app/saha-release.keystore
android/app/*.keystore
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/

# APK çıktıları (public/apk hariç — production dağıtım)
*.apk
!public/apk/saha-latest.apk
